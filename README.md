#website

i like pizza

and also i like hotdog

```
#view changed files
git status
#view diff
git diff
#add files to staged
git add somefile.txt
#commit staged files
git commit -m "comment"
#push changes to remote repository
git push
```
